name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: 76.13.153.215
          username: ubuntu
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 20m
          script: /docker/regionfinder/deploy.sh
